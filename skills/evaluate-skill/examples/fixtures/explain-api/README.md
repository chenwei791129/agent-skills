# Example Fixture

This fixture describes a tiny public API for evaluation examples.

```python
def greet(name: str) -> str:
    """Return a greeting for name."""
```

Call `greet` with a non-empty string. The return value is `Hello, <name>!`.
