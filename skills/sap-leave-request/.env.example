# SAP SuccessFactors 登入帳號（必填）。
# 帳號通常帶網域反斜線（DOMAIN\account），務必用「單引號」包住，
# 否則 source .env 會把 \ 當跳脫字元吃掉，導致登入失敗。
SAP_USERNAME='DOMAIN\your.account'

# 登入密碼（必填）。同樣建議用單引號，避免特殊字元被 shell 解讀。
SAP_PASSWORD='your-password'

# 公司的 SuccessFactors 首頁網址（必填）。
# 主機（SAP 資料中心，如 hcm-apXX...）與 bplte_company 公司代碼因組織而異，
# 登入成功後瀏覽器應停在這個位址。
SAP_URL='https://<your-sf-host>/sf/home?bplte_company=<your-company>'

# 憑證查找順序：執行目錄的 .env > --env 指定的路徑。
# 複製本檔為 .env 並填入後即可使用；不要把真實 .env 提交進版控。
