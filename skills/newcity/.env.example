# Newcity 社區 app 登入帳號密碼（必填）
NEWCITY_USERID=your_userid_here
NEWCITY_PASSWORD=your_password_here

# 選填：Bearer header token。
# 留空則每次執行自動隨機產生（API 不驗證此值，可正常查詢）；
# 若日後後端開始驗證，可從 app 封包擷取真實值後填入此處覆寫。
# NEWCITY_BEARER=

# 憑證查找順序（高 -> 低）：執行目錄的 .env > 此 skill 目錄的 .env > 環境變數。
# 複製本檔為 .env 並填入後即可使用；不要把真實 .env 提交進版控。
